task cleanTempDirectory {
	File tmp = file('temp')

	onlyIf { 	
		tmp.exists()
	}
	
	doLast {
		tmp.delete()
        println "deleted temp"
    }
}

task encryptFile << {

    println "first Action"

    //  stop its own execution and to start execution of the task's next action
    throw new StopActionException()

    println "will not be printed"
}

encryptFile << {

    println "second Action"

    // stop execution of the current task
	throw new StopExecutionException()	
}

encryptFile << {
	
	println "You will never see this message"
}